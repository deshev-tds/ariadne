## Recovery Note: 2026-03-26

This repo was split into two lines on March 26, 2026:

- `archive/web-smartness-pre-revert-20260326`
- `recovery/47ec-parent-localcorpus-offsec-clean`

The recovery branch is intentionally based on:

- `47ec9b693^` = `124961bde99a1d61861c38ef0432d5465d9fb157`

That keeps the earlier `17023d0f0` local-first strong-source work in history, but drops the later two-stage web-evidence layer that taught models to think in stored artifacts, snippet follow-ups, and re-query choreography.

### What Was Kept

- domain-first local corpus tools
- local-corpus reasoning packs and auto shelf inspection
- science/offsec working modes
- Offsec corpus consult and retrieval
- guided Offsec terminal loop

### What Was Intentionally Not Carried Forward

- the post-`47ec9b693` web evidence store / snippet retrieval path
- the assumption that a model will reliably treat multi-stage stored-page workflows as "I have already read the source"
- web-smartness layers that silently rewrote or escalated discovery flows away from a simple `search -> fetch -> answer` mental model

### Cautionary Tale

The failure mode was not just "retrieval bug" or "bad model".

The stack gradually moved from:

- find source
- fetch source
- answer from fetched text

to something closer to:

- find source
- store source artifact
- query snippets from the stored artifact
- infer whether more slabs are needed
- carry extra selector and evidence-routing semantics in middleware

That design was attractive because it promised:

- better source discipline
- less prompt bloat
- more explicit evidence control

But in practice the models repeatedly showed the opposite behavior:

- they treated full fetches as if they were only previews
- they kept searching after already receiving the relevant answer span
- they drifted into storage or note-lookup mental models
- native function-calling became easier to derail because the tool contract stopped resembling a normal `open page / read page` pattern

### Lesson

If a lane must be dependable across different open models, keep at least one path boring:

- simple tool surface
- simple schemas
- direct fetch semantics
- no silent upgrades
- no hidden "actually this is a stored artifact" mental trap

More ambitious evidence workflows should live behind explicit specialized lanes and should be judged against a still-working vanilla control lane, not replace it.
